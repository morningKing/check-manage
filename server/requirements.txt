# check-manage backend dependencies (server/). Install: pip install -r requirements.txt
# Note: the MCP service (mcp-server/) has its own venv and dependencies, not listed here.
# Keep this file ASCII-only: on Chinese Windows pip reads it as cp936/GBK and
# non-ASCII (e.g. Chinese) comments raise UnicodeDecodeError.

# --- Web framework / server ---
Flask>=3.0.0
flask-cors>=4.0.0
waitress>=3.0.0            # production WSGI server (proxy.py); dev uses Werkzeug

# --- Database ---
psycopg2-binary>=2.9.0

# --- Auth / config ---
PyJWT>=2.8.0
python-dotenv>=1.0.0

# --- HTTP client (OpenCode / LLM / outbound webhooks) ---
requests>=2.31.0

# --- Scheduler (backups / dependency checks / AI scans) ---
APScheduler>=3.10.0

# --- AI long-term memory (mem0 + embedded vector store) ---
mem0ai>=0.1.0
chromadb>=0.5.0

# --- Script sandbox preinjection + data processing + Excel export ---
pandas>=2.0.0             # preinjected as pd in script_runner
numpy>=1.24.0             # preinjected as np in script_runner
openpyxl>=3.1.0           # .xlsx export for operation_logs / data_export

# --- Testing ---
pytest>=7.4.0
pytest-cov>=5.0.0
