# ETL 任务

ETL（抽取 → 转换 → 加载）任务把数据从来源（其它集合、第三方 HTTP 源，或直接上传的 Excel/CSV 文件）抽取出来，经字段映射 / 条件过滤 / 转换后写入目标集合。入口为 **设置中心 → 数据运维（`/admin/data-ops`）→ ETL 管理**，需要管理员权限。

## 任务组成

| 步骤 | 说明 |
|------|------|
| **抽取（Extract）** | 从来源集合、第三方 HTTP 接口读取数据，或上传 Excel（.xlsx/.xls）/ CSV 文件 |
| **转换（Transform）** | 字段映射（源字段 → 目标字段）、条件过滤、值转换 |
| **加载（Load）** | 写入目标集合（`dynamic_data`） |

## 创建任务

1. 进入 **数据运维 → ETL 管理**，点击 **新建任务**。
2. 配置来源：选择源集合，或填写第三方 HTTP 源地址。
   - 若来源是文件，新增"文件上传"步骤并上传 `.xlsx`/`.xls`/`.csv` 文件（首行作为字段名）；文件在配置时上传并固定绑定，之后每次运行该任务都会重新解析同一份文件。
3. 配置 **字段映射**：将来源字段对应到目标集合的字段。
4. 配置 **过滤条件**（可选）：仅处理满足条件的记录。
5. 选择 **目标集合**，保存。

## 运行与调试

- **测试运行**：只取样本（默认前 50 条，不写库）快速跑一遍整个管道，用于设计阶段调试。每一步的结果都会展开一个"样例数据"表格，可以直接看到这一步处理后的记录长什么样，不用等正式运行才知道字段映射/过滤条件写得对不对。
- **正式运行**：点击「执行」后立即返回，不会卡住页面——数据在后台异步处理，页面实时轮询展示进度（当前在跑哪一步、写入集合的第几条/共几条），并提供「取消运行」按钮。取消后已经成功写入的批次会保留，不会整体回滚；已处理的部分不需要重新执行。
- **执行日志**：`etl_logs` 记录每次运行的处理条数、成功 / 失败、错误信息，状态包括排队中 / 运行中 / 成功 / 部分成功 / 失败 / 已取消，可在任务详情查看。

## 提示

- 字段映射使用记录的**原始字段名**（非显示标签）。
- 大批量抽取时注意目标集合的主键唯一性与并发写入约束。
