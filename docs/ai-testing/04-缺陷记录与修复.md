# AI 测试缺陷记录与修复

> 记录约定：发现即修（修复 + 回归测试同一次提交）。状态：Fixed / Accepted（文档化的刻意行为）/ Open。

## Fixed（本轮修复）

| # | 等级 | 位置 | 现象 | 根因 | 修复 | 回归 |
|---|------|------|------|------|------|------|
| D1 | 高 | `routes/ai_chat_batches.py` create/append | UI 批任务创建/追加不校验 `files[].path`：绝对路径或 `batch-staging/<他人>/...` 会被 worker 拷进自己工作区（跨用户任意文件读取） | 内部接口零校验（对外 API 有 `_validate_staged_path`，内部没有） | `utils/workspace.validate_staged_files`：路径必须相对、无 `..`、落在本人 staging 下且文件存在；create/append 两处接入 | `test_internal_staging_guard.py`（7 例） |
| D2 | 中 | `routes/open_api_batches.py` session_file_download / download-all | 对外下载用 `normpath+commonpath` 弱校验，不解析符号链接：工作区内指向外部的 symlink 可被读出；跨盘 `commonpath` 还会抛 ValueError→500 | 与内部端点（`safe_resolve`）双实现漂移 | 两处统一改走 `utils.workspace.safe_resolve`（resolve 后 containment） | `test_open_api_download_guard.py`（6 例，symlink 2 例无权限环境自动跳过） |
| D3 | 中 | `utils/upload_limits.py` | `/v1/ai-sessions`、`/v1/memories`、`/v1/prompt-templates`、`/v1/ai-scan-tasks`、行操作 run 无请求体上限，超大 JSON 会被代理整体读入内存 | `body_limit_for_path` 只匹配 ai-batches 前缀 | 扩展 JSON 前缀族 + 行操作 run 精确匹配；`/v1/collections` 通用数据接口与备份还原保持不限 | `test_upload_limits_extended.py`（5 例） |
| D4 | 中 | `utils/batch_engine.py` standalone 分支 + `_maybe_auto_retry`/`_requeue_lost` | `/v1/ai-sessions` 独立会话在 create_session 阶段网络失败后自动重排，`continue_prompt` 被置 NULL → 重跑时 prompt 为空串，任务空跑 | 认领时立即清空 prompt + 重排队 CASE `ELSE NULL` 双重作用 | standalone 认领保留 continue_prompt（所有重新认领路径都应取回原始请求；reexecute 语义随之修正）；重排队 CASE `ELSE continue_prompt`（oc 为 NULL 时不再清空） | `test_batch_standalone_prompt_retry.py`（3 例，真实 DB 链路） |
| D5 | 低 | `routes/ai_memory_internal.py::internal_search` | `limit` 非数字/None → 500（内部通道，但 500 不符合错误契约） | `int(body.get('limit', 5))` 无保护 | None→默认 5；非数字→400 中文错误；<1→回落 5 | `test_ai_memory_internal_limit.py`（4 例） |
| D6 | 低 | `routes/ai_chat.py::_maybe_bucket_legacy_sessions` | 计数查询 `fetchone()[0]` 无防御，mock/异常驱动下 TypeError | fetchone 结果直接下标 | `(row[0] if row else 0)` 防御取值 | `test_routes_ai_chat.py::test_list_sessions_returns_user_sessions` 等 |
| D7 | 低 | `tests/test_ai_scan_writeback.py`、`test_ai_chat_governance.py`、`test_routes_ai_chat.py` | 4 个存量用例失败（代码演进后夹具未同步：session 行缺 `id` 键、列表行 6 元组 vs 现行 8 元组、分组查询新增 created_at） | 代码加了执行审计校验器/分组置顶字段，夹具没跟上 | 夹具对齐现行 SELECT 形状；`test_list_sessions_returns_user_sessions` 顺带补 `lastActiveAt`/`groups` 断言 | 4 个用例转绿（116 passed in 3 files） |

## Accepted（核实为文档化刻意行为，不改）

| # | 位置 | 行为 | 依据 |
|---|------|------|------|
| A1 | 对外 `DELETE /v1/ai-batches/<id>` | 无终态门，运行中也可删（引擎侧 batch context None 兜底） | 对外 API 文档删除语义「不可逆删除」；与 UI 的 409+stop=1 差异已在 `ai-batch-api.md` 标注 |
| A2 | `routes/ai.py::put_settings` 掩码回退 `set(api_key[:-4])=={'*'}` | 真实密钥前 n-4 位全为 `*` 的极端输入会被误判为未修改 | DashScope 密钥字符空间下概率可忽略；新增行为回归测试见 `test_routes_ai_settings.py`（既有） |
| A3 | 对外 scan run-now 不检查 `task.enabled` | 与内部 run-now 行为一致（对外不引入内部没有的新规则） | `ai/scan-tasks.md` §10 |
| A4 | 批任务终态可被 cancel/append/retry/continue/reexecute 拉回 → 回调可多次触发 | 每次收敛都是真实完成事件，刻意不去重 | `ai-batch-api.md` 回调章节 |
| A5 | PATCH（内部+对外）整体替换语义 | 漏传字段清空为默认；对外文档 4.2a 显著提醒 | 文档化行为，回归测试覆盖 |

## 备注

- D1–D5 属于「AI OpenAPI / 批任务」安全与健壮性缺陷，均由代码审查发现、用例先行复现后修复。
- D6/D7 由全量基线跑测发现（基线：4 failed / 2062 passed / 1 skipped，6m09s），符合「测出即修」约定。
