# 域⑨ AI 智能助手

> 架构图对应：业务域 ⑨（AI 智能助手）+ 右侧 AI 子系统（OpenCode / MCP Server）。索引见 [`README.md`](./README.md)。

## 1. 模块职责

平台与 AI 能力的唯一桥梁。提供三种形态：**对话助手**（流式、工具调用、制品）、**批任务**（N 文件并发处理）、**定时 AI 流水线**（扫描数据页→AI 处理→结构化回写）。核心理念：**浏览器只与 Flask 通信**；Flask 作为网关与 SSE 代理对接 **OpenCode** Agent 运行时；平台能力经**独立 MCP Server** 暴露给 Agent，且受同一套 RBAC 约束。"加 skill 即获得能力，前端零修改"。

## 2. 模块组成

| 蓝图/工具 | 职责 |
|-----------|------|
| `ai` | AI 全局配置（`ai_settings`）+ **长期记忆**端点（`/ai/memories` 查/增/删） |
| `ai_chat`（`/ai/chat`） | 会话/消息/SSE 代理/文件/运行脚本；会话治理（close/reopen/admin archive）+ 审计 |
| `ai_chat_batches` | 批任务 CRUD、暂存上传、重试 |
| `ai_chat_prompt_templates` | Prompt 模板 CRUD（按用户） |
| `ai_scan_tasks` | 定时扫描任务 CRUD、run-now（`admin.ai_scan`） |
| `ai_memory_internal`（`/ai/memory/internal/*`） | 供 MCP 记忆工具回连的内部端点（`MCP_INTERNAL_TOKEN` 鉴权）；Flask 是向量库唯一写入方 |
| `utils/memory.py` | **长期记忆引擎**：mem0（Chroma 向量库 + 嵌入/提炼）按 `user_id` 管理跨会话记忆；所有原生调用钉单线程 executor |
| `utils/batch_engine.py` | 批任务 Worker（守护线程 + 线程池3） |
| `utils/ai_scan_engine.py` · `ai_scan_scheduler.py` | 扫描引擎 + 调度器 |
| `utils/opencode_client.py` · `workspace.py` · `session_token.py` | OpenCode 封装 / 每会话工作区 / opaque 令牌 |
| **mcp-server/**（独立进程） | FastAPI + MCP Streamable-HTTP，暴露平台工具（含记忆工具 `memory_search/add/delete`） |

前端：`views/ai-chat/AiChatView.vue`、`BatchListView`/`BatchDetailView`、`components/ai-chat/`（CreateBatchDialog/PromptTemplateManager/ArtifactCard…）、`stores/aiChat*.ts`、`views/admin/AiScanTaskManager.vue`。

## 3. 外部子系统（独立进程）

| 进程 | 地址 | 职责 |
|------|------|------|
| OpenCode | `opencode serve` :4096 | Agent 编排、工具调用、SSE 事件流、调用 LLM Provider |
| MCP Server | :3003 | 向 Agent 暴露平台工具，token→DB 校验得用户/RBAC，直连 DB |
| LLM Provider | 外部 | Claude / MiMo 等大模型，由 OpenCode 内部调用 |

> **Agent 与模型可选**：对话/批任务/定时任务均可指定 OpenCode Agent（如 `build`/`plan`/`general`/`explore` 及子智能体），留空用默认；模型经 `OPENCODE_MODEL`（默认空 → OpenCode 选首个 provider 默认模型）。Agent 列表来自 `/ai/chat/agents`（需 OpenCode 在线）。

## 4. 数据模型

- **`ai_chat_sessions`** — `id`、`user_id`、`title`、`opencode_session_id`、`workspace_path`、`session_token`(唯一 opaque)、`token_expires_at`、`status`；批任务子会话另有 `batch_id`/`batch_seq`/`batch_input_file`；扫描子会话另有 `scan_task_id`/`source_record_id`。
- **`ai_chat_messages`** — `session_id`、`role`、`content` JSONB(typed parts：text/file/tool_use/run_result)。
- **`ai_chat_batches`** — `name`、`prompt`、`template_id`、`total/done/failed`、`status`、**`agent`**。
- **`ai_chat_prompt_templates`** — 按用户的 Prompt 模板。
- **`ai_scan_tasks`** — 扫描配置（collection、状态字段与各状态值、过滤、提示词模板、字段映射、调度间隔、每次条数、**agent**）。
- **`ai_settings`** — AI 全局配置：LLM `api_key`/`endpoint`/`model`、超时/`max_tokens`，以及**长期记忆**开关 `mem0_enabled` 与嵌入模型 `embedding_model`（默认 `text-embedding-v3`）。
- **长期记忆向量库（非 PG）** — mem0 用 **Chroma 嵌入式向量库**持久化在 `MEM0_STORE_ROOT`（默认 `~/.check-manage/mem0/`，备份时归入 `vector_store/`）。记忆**按 `user_id` 分区**（同一固定 collection `memories`，靠 `user_id` 过滤区分用户），不分会话——会话只是产生记忆的现场。条目结构为「事实文本 + 嵌入向量 + `user_id` 元数据」。

## 5. 核心流程

### 5.1 对话（SSE 流式）
1. **建会话**：建仓库外 workspace → 落库 → 生成 opaque 令牌 → 写 `opencode.json`（含 MCP url `?token=` 与模型）→ `POST /session?directory=` → 回填 OpenCode 会话 id。
2. **发消息**：落库 user 消息（附件文本内联）→ `send_prompt_async(parts, model, agent, agent_parts)`。
3. **流式**：浏览器 `EventSource` 订阅 Flask SSE 代理（带 `?access_token=`，OpenCode 全局 `/event` 按 `sessionID` 过滤）→ store 按 part id 覆盖累积（text/reasoning/tool）→ `session.idle` 落库 assistant 文本、刷新 outputs。
4. **身份链路**：Flask 令牌 → workspace `opencode.json` → OpenCode 带令牌连 MCP → MCP 查表得 `user_id/role` → 工具 RBAC。
5. **制品**：助手大代码/文档块前端提取为制品卡（同名跨消息归为多版本）；HTML/SVG 沙箱 iframe 渲染；Python 制品可手动「运行」，结果作为 `run_result` 持久化。

### 5.2 批任务
1. 选 N 文件 + 1 Prompt（可选模板/Agent）→ 创建一个 `ai_chat_batches` + N 个 `pending` 子会话。
2. **BatchWorker**（守护线程 + `ThreadPoolExecutor(3)`）FIFO `FOR UPDATE SKIP LOCKED` 认领子会话，逐个 `create_session → send_message(agent=) → 等待完成 → 落库`，30 分钟/子会话超时。
3. 失败子会话红旗标记、不中止整批；`retry-failed` 把失败重置为 `pending`。Flask 重启时审计把孤儿 `running` 重置 `pending`。
4. 模板下拉每次展开/打开懒加载，确保"管理模板"新建后即可选到。

### 5.3 定时 AI 流水线（复用批引擎）
1. `ai_scan_scheduler`（APScheduler 1 分钟 tick，per-task 到期/锁、启动孤儿清扫）触发到期任务。
2. `ai_scan_engine` 原子 `FOR UPDATE SKIP LOCKED` 认领待处理记录 → 翻转"处理中"；上下文目录 = `record.md` + `attachments/`；prompt = 模板 + 按字段映射自动追加的 JSON 契约。
3. 每次扫描 = 一个 batch，每条记录 = 一个子会话（带 `scan_task_id`/`source_record_id`/`agent`）；批任务完成钩子 `batch_engine._run_one` 回调 `ai_scan_engine.on_child_finished` 解析 AI 的结构化 JSON，经参数化 `jsonb_set` 回写记录，状态流转 待处理→处理中→已处理/处理失败。
- 跨表副作用交给 skill 经 MCP；仅同行结构化回写内建。

### 5.4 长期记忆（mem0）

平台为真人对话提供**跨会话长期记忆**，按 `user_id` 沉淀偏好与关键事实，使助手"记得用户"。记忆层全程降级——异常一律 no-op，绝不影响聊天主链路。开关 `ai_settings.mem0_enabled`（默认关），未配 `api_key` 或未启用则整层为空操作。

- **注入（读）**：`send_message` 转发给 OpenCode 前先 `search_memory(user_id, 本轮问题)` 做向量相似度召回 top-k，拼成记忆块前置注入提示词。
- **被动写（自动）**：SSE `session.idle`、`persist_turn` 之后由 `extract_from_turn` **后台线程**抽取这一轮 `[user, assistant]`——mem0 内部再调一次 LLM **提炼成简洁规范事实**并自行决定 ADD/UPDATE/NOOP。仅真人会话抽取（`batch_id`/`scan_task_id` 为空者跳过）。
- **主动写**：① 用户在「我的记忆」抽屉**手动补写** `POST /ai/memories`（默认 `infer=True` 走 mem0 提炼，勾「原样保存」→ `infer=False` 原文照存、仍嵌入可检索）；② Agent 经 MCP `memory_add` 显式补写。两者最终都落到同一按 `user_id` 分区的 mem0 库。
- **管理**：`GET/DELETE /ai/memories` 查看/删除本人记忆（删前校验归属）；不受启用开关约束（只要配了 `api_key` 即可用）。
- **原生线程亲和（关键约束）**：Chroma 经 onnxruntime（原生 C++）做嵌入，其原生态**绑定创建线程**，跨线程使用会 SIGSEGV 整个进程（且段错误在 Python 兜底前发生、接不住）。故 `memory.py` 用 `max_workers=1` 的线程池作"记忆专属线程"，init 与所有 add/search/get_all/delete 都钉在同一条线程上，调用方仍是同步语义。
- **韧性恢复（M3）**：底层 OpenCode 会话失效时 `_recover_session_and_resend` 重建会话上下文（注入最近历史摘要）后重发，使重开/续聊不丢上下文。

## 6. 关键接口

| 路由 | 方法 | 鉴权 |
|------|------|------|
| `/ai/chat/sessions` `/<id>` `/messages` `/events` `/files` `/run` | CRUD/SSE | login / write_required / login_required_sse |
| `/ai/chat/agents` | GET | login（OpenCode 在线时返回 agent 列表） |
| `/ai/chat/batches` `/<id>` `/retry-failed` `/staging/upload` | CRUD/POST | login |
| `/ai/chat/prompt-templates` ... | CRUD | login |
| `/ai-scan-tasks` `/<id>` `/run-now` | CRUD/POST | admin.ai_scan |
| `/ai/memories` | GET/POST/DELETE | login（POST 手动补写，body `{text, verbatim?}`；DELETE 校验归属；mem0 未配置 POST 返回 409） |
| `/ai/memory/internal/*` | POST | MCP_INTERNAL_TOKEN（供 MCP 记忆工具回连，Flask 仍是向量库唯一写入方） |
| **MCP 工具**（OpenCode 调用） | — | token+RBAC | `list_collections` / `save_artifact` / `read_upload` / `export_collection_excel` / `run_python` / `memory_search` / `memory_add` / `memory_delete` |

## 7. 依赖与协作关系

- **依赖**：[域①](./01-认证与权限.md)（会话 `write_required`、MCP 经令牌反查 RBAC）、[域③](./03-动态数据核心.md)（扫描读写记录）、[域②](./02-配置中心.md)（字段配置）。
- **外部集成**：OpenCode（SSE 代理）→ MCP Server（工具，回连 DB 受 RBAC）→ LLM Provider（推理）；长期记忆经 OpenAI 兼容端点（DashScope）做**嵌入与提炼**。
- **被 [域⑧运维监控](./08-运维监控.md) 备份**：长期记忆向量库 `MEM0_STORE_ROOT`（`vector_store/`）与数据文件 `DATA_FILES_ROOT`（`data_files/`）随系统备份整目录打包/还原（`include_vector_store`/`include_data_files` 默认开），使记忆数据与 PG 里的引用 id 在同一份备份里配套一致。
- **复用**：定时流水线复用批任务引擎；前端动态路由对 `/ai-chat` 放行非访客（与后端 `@write_required` 对齐）。

## 8. 安全与隔离

- **令牌**：opaque、每会话唯一、可吊销、有 TTL；删会话先吊销令牌+标记删除，再尽力清目录。
- **工作区隔离**：`AI_WORKSPACE_ROOT/<user>/<session>/` 在**仓库外**——OpenCode 内置文件工具 cwd 是仓库根，故扫不到任何会话上传；访问上传只能经 `read_upload`（会话作用域）或服务端内联（文本、200KB 上限）。
- **路径穿越防护**：`safe_resolve`/`secure_filename`/`commonpath`。
- **密钥**：LLM provider 密钥只存 OpenCode 全局配置，不进 git。

## 9. 设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 接入方式 | 对接 OpenCode 而非直连 LLM | 获得 Agent 编排/工具/skill 生态，前端零改动加能力 |
| 浏览器边界 | 只连 Flask | 不暴露 OpenCode/MCP，统一鉴权与代理 |
| MCP 解耦 | 独立进程 + 独立 venv | 与 Flask 隔离，token→RBAC 复用平台权限 |
| 工作区 | 仓库外 + 每会话隔离 | 防 Agent 内置工具越权读其它会话/仓库 |
| 定时流水线 | 复用批任务引擎 | 一套并发/认领/超时机制，扫描=batch、记录=子会话 |
| Agent/模型 | 可配置（下拉/字段） | 不锁定单一模型，部署可换 provider |
| 长期记忆 | mem0 + 嵌入式 Chroma，按 `user_id` | 现成的提炼/去重/检索；本地持久化、可随系统备份；不引入额外服务进程 |
| 记忆写入线程 | 全部钉单线程 executor | Chroma/onnxruntime 原生态线程亲和，跨线程使用会 segfault 进程（兜底接不住）|
| 记忆默认关 | `mem0_enabled` 默认 FALSE | opt-in：避免默认对每个部署/用户产生抽取的 token 成本与隐私采集 |
| 手动补写 | 默认提炼 + verbatim 开关 | 补救自动抽取遗漏；verbatim 适合一句话关键事实（仍嵌入可检索，但不去重，宜短而原子）|
| Flask 唯一写入方 | MCP 记忆工具回连 Flask 内部端点 | Chroma 是单写存储，统一由 Flask 写，避免多进程并发写冲突 |
